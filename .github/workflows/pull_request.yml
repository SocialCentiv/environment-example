name: Pull Request Workflow

on: pull_request

jobs:
  greet:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Echo greeting
        run: echo "hello ${{ vars.AWS_ACCOUNT_ID }} AWS Account!"
